# คู่มือการตั้งค่า LINE Messaging API สำหรับส่งแจ้งเตือนงาน

## 1. สร้าง LINE Messaging API Channel

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. เลือก Provider ของคุณ
3. สร้าง Channel ใหม่ประเภท **Messaging API**

## 2. ตั้งค่า Messaging API Channel

### **Basic settings**
- Channel name: Gig Glide Notifications
- Channel description: แจ้งเตือนงานใหม่สำหรับนักดนตรี
- Icon: อัปโหลดโลโก้ Gig Glide

### **Messaging API settings**
- **Webhook URL**: ไม่ต้องตั้งค่า (เราใช้แค่ Push API)
- **Allow bot to join group chats**: ปิด
- **Auto-reply messages**: ปิด
- **Use webhook**: ปิด

## 3. รับ Channel Access Token

1. ในหน้า Channel settings → **Messaging API**
2. คลิก **Issue** ที่ **Channel access token**
3. เลือก **Long-lived token**
4. คัดลอก **Channel access token** ที่ได้

## 4. ตั้งค่า Environment Variables

เพิ่มในไฟล์ `.env`:

```bash
# LINE Configuration
VITE_LINE_CLIENT_ID=YOUR_LINE_LOGIN_CHANNEL_ID
VITE_LINE_CLIENT_SECRET=YOUR_LINE_LOGIN_CHANNEL_SECRET
VITE_LINE_ACCESS_TOKEN=YOUR_MESSAGING_CHANNEL_ACCESS_TOKEN
```

## 5. ทดสอบระบบแจ้งเตือน

### ทดสอบ API ด้วย curl:

```bash
curl -X POST http://localhost:8080/api/notify-new-job \
  -H "Content-Type: application/json" \
  -d '{
    "jobData": {
      "id": "test-job-123",
      "title": "หามือกีตาร์",
      "instrument": "กีตาร์",
      "venue": "ร้านดนตรีแห่งหนึ่ง",
      "province": "กรุงเทพมหานคร",
      "date": "15/03/2026",
      "time": "20:00",
      "budget": "2000 บาท"
    }
  }'
```

## 6. การทำงานของระบบ

### **Trigger (การทำงาน)**
- เมื่อมีการเพิ่มงานใหม่ในตาราง `jobs`
- ระบบจะเรียก API `/api/notify-new-job` อัตโนมัติ

### **Target (กลุ่มเป้าหมาย)**
- ดึง `line_user_id` จากตาราง `profiles`
- ส่งเฉพาะผู้ใช้ที่เชื่อมต่อ LINE ไว้แล้ว

### **Action (การส่งข้อความ)**
- ใช้ LINE Messaging API Push
- ส่ง Flex Message แบบการ์ดสวยงาม
- รวมลิงก์กลับมาที่เว็บไซต์

## 7. รูปแบบข้อความที่ส่ง

Flex Message ประกอบด้วย:
- **Header**: 🎵 งานใหม่! + Gig Glide
- **Hero**: ชื่อตำแหน่ง (เช่น กีตาร์)
- **Body**: รายละเอียดงาน (สถานที่, วันเวลา, ค่าจ้าง)
- **Footer**: ปุ่ม "ดูรายละเอียดงาน" + "เปิดแอป Gig Glide"

## 8. การจัดการข้อผิดพลาด

ระบบจะ:
- ทำงานต่อแม้ LINE ส่งไม่สำเร็จ
- บันทึก log ไว้ตรวจสอบ
- ไม่ทำให้การสร้างงานล้มเหลว

## 9. ข้อควรระวัง

- **Access Token** มีความลับสูง อย่าแชร์
- **Rate Limit**: LINE จำกัดจำนวนข้อความที่ส่งได้
- **Cost**: การส่งข้อความอาจมีค่าใช้จ่าย
- **User Privacy**: ส่งข้อความเฉพาะผู้ที่ยินยอมเชื่อมต่อ LINE

## 10. ตรวจสอบสถานะ

ตรวจสอบได้จาก:
- Console log ใน browser
- Server log ใน terminal
- ตรวจสอบจำนวนคนที่ได้รับการแจ้งเตือน

## 11. ปิดใช้งาน (ถ้าต้องการ)

แค่ลบ `VITE_LINE_ACCESS_TOKEN` ออกจาก `.env` ระบบจะข้ามการส่งแจ้งเตือน
