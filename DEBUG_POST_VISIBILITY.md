# แก้ไขปัญหาไม่เห็นโพสต์ของตัวเองในหน้าประกาศ

## สิ่งที่ได้แก้ไข:

### 1. ✅ เพิ่ม Debugging Logs
- **App.tsx**: Log session ID, query results, และ fallback results
- **NearbyGigs.tsx**: Log jobs ที่ได้รับ, user ID, และงานของตัวเอง

### 2. ✅ ปรับ Query Logic
- **ปรับ OR condition**: เปลี่ยนจาก `status.eq.open,status.is.null,user_id.eq.{userId}` 
  เป็น `user_id.eq.{userId},status.eq.open,status.is.null` (ให้ user_id มาก่อน)
- **Fallback query**: ใช้ logic เดียวกันทั้ง join และ fallback

### 3. ✅ ปิดการกรองระยะทางชั่วคราว
- **ไม่มี distance filtering**: ตรวจสอบแล้วว่าไม่มีการกรองตาม latitude/longitude
- **แสดงทุกงาน**: ชั่วคราวเพื่อทดสอบว่าโพสต์ปรากฏหรือไม่

### 4. ✅ ใส่กรอบสีส้มสำหรับงานของตัวเอง
- **Visual indicator**: กรอบสีส้ม + พื้นหลังอ่อน
- **Badge "ประกาศของคุณ"**: แสดงว่าเป็นงานของเรา
- **ปุ่มจัดการ**: แสดงปุ่ม "ดูรายชื่อผู้สมัคร" สำหรับเจ้าของงาน

## วิธีทดสอบ:

### 1. เปิด Console ดู Debug Logs:
1. เปิด Developer Tools (F12)
2. ไปแท็บ Console
3. รีเฟรชหน้า "งานคืนนี้ใกล้คุณ"
4. ดู logs ที่แสดง:
   ```
   Current session: [user-id]
   Jobs query result: [jobs-array]
   NearbyGigs received jobs: [number] [jobs-array]
   User's own jobs: [number] [user-jobs-array]
   ```

### 2. ตรวจสอบ Query:
- **ถ้าเห็น user ID**: แสดงว่า session ถูกต้อง
- **ถ้าเห็น jobs**: แสดงว่า query ทำงาน
- **ถ้าเห็น user's own jobs**: แสดงว่างานของคุณอยู่ในรายการ

### 3. ตรวจสอบ UI:
- **งานของคุณ**: ควรมีกรอบสีส้ม + badge "ประกาศของคุณ"
- **ปุ่มจัดการ**: ควรเห็นปุ่ม "ดูรายชื่อผู้สมัคร"

## สาเหตุที่อาจเกิดปัญหา:

### 1. Session Issue:
- **ไม่มี session**: ตรวจสอบว่าล็อกอินอยู่
- **Session หมดอายุ**: ล็อกอินใหม่

### 2. Query Issue:
- **OR condition ไม่ถูกต้อง**: แก้ไขแล้วให้ user_id มาก่อน
- **Status field**: ถ้าไม่มี status field จะใช้ null ได้

### 3. Database Issue:
- **RLS policies**: ตรวจสอบว่ามีการตั้งค่า RLS ถูกต้อง
- **Foreign key**: ตรวจสอบว่า user_id ถูกต้อง

## ขั้นตอนแก้ไขถ้ายังไม่ได้:

### 1. ตรวจสอบ Console:
- ดูว่ามี error หรือไม่
- ตรวจสอบว่า user ID ถูกต้องหรือไม่

### 2. ตรวจสอบ Database:
```sql
-- ตรวจสอบว่ามีงานของคุณจริงหรือไม่
SELECT * FROM jobs WHERE user_id = 'your-user-id';

-- ตรวจสอบ status field
SELECT id, user_id, status FROM jobs WHERE user_id = 'your-user-id';
```

### 3. ทดสอบ Query ตรงๆ:
```sql
-- ทดสอบ query ใน Supabase SQL Editor
SELECT * FROM jobs 
WHERE user_id = 'your-user-id' OR status = 'open' OR status IS NULL
ORDER BY created_at DESC;
```

## หมายเหตุ:
- **Debug logs**: จะช่วยให้เห็นว่าเกิดอะไรขึ้น
- **Temporary override**: แสดงทุกงานชั่วคราวเพื่อทดสอบ
- **Visual indicators**: ช่วยให้มั่นใจว่าเห็นงานของตัวเอง

ลองทดสอบและดู console logs ก่อน จะช่วยระบุปัญหาได้แม่นยำ! 🎵✨
