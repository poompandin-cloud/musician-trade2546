# 🚨 แก้ไขปัญหาเครดิตไม่หักเมื่อประกาศงาน

## 🔍 ปัญหา:
- ❌ ประกาศงานแล้วแต่เครดิตไม่หัก
- ❌ ไม่มีการเปลี่ยนแปลงในระบบ
- ❌ UI ไม่อัปเดต

## 🔧 การตรวจสอบและแก้ไข:

### 1. ✅ เพิ่ม Debug Logs ใน addJob
```tsx
// 4. หลังจากบันทึกงานสำเร็จ ให้ทำการหักเครดิตออก 5
const newBalance = currentCredits - 5;
console.log("🔍 กำลังจะหักเครดิต:", { currentCredits, newBalance, userId });

const { data: updateData, error: updateError } = await supabase
  .from('profiles')
  .update({ credits: newBalance })
  .eq('id', userId)
  .select()
  .single();

console.log("🔍 ผลการอัปเดตเครดิต:", { updateData, updateError });
```

### 2. ✅ ตรวจสอบคอลัมน์ในฐานข้อมูล
รัน SQL script: `CHECK_CREDITS_COLUMN.sql`

```sql
-- ตรวจสอบว่ามีคอลัมน์ credits หรือไม่
SELECT column_name 
FROM information_schema.columns 
WHERE table_name = 'profiles' AND column_name = 'credits';

-- ถ้าไม่มีให้เพิ่ม
ALTER TABLE profiles ADD COLUMN credits INTEGER DEFAULT 20;

-- ถ้ามี credit_balance แต่ต้องการเปลี่ยน
ALTER TABLE profiles RENAME COLUMN credit_balance TO credits;
```

### 3. ✅ ตรวจสอบ RLS Policies
```sql
-- ตรวจสอบว่ามี policy ที่อนุญาตให้ update ไหม
SELECT * FROM pg_policies WHERE tablename = 'profiles';

-- ถ้าไม่มีให้เพิ่ม
CREATE POLICY "Users can update own profile" ON profiles
  FOR UPDATE USING (auth.uid() = id);
```

### 4. ✅ ทดสอบการทำงาน
1. **เปิด Console** ใน browser
2. **ลงประกาศงาน** และดู logs:
   ```
   🚀 เริ่มต้นกระบวนการประกาศงาน...
   🔍 เครดิตปัจจุบันใน DB: 20
   ✅ บันทึกงานสำเร็จ ID: xxx
   🔍 กำลังจะหักเครดิต: {currentCredits: 20, newBalance: 15, userId: "xxx"}
   🔍 ผลการอัปเดตเครดิต: {updateData: {...}, updateError: null}
   ✅ หักเครดิตสำเร็จ! ยอดคงเหลือ: 15
   🔄 กำลังอัปเดตหน้าจอทันที...
   ✅ อัปเดตหน้าจอสำเร็จ!
   ```

## 🚨 สิ่งที่ต้องตรวจสอบ:

### **A. คอลัมน์ในตาราง profiles**
- ✅ มีคอลัมน์ `credits` หรือไม่?
- ✅ ชนิดข้อมูลเป็น `INTEGER` หรือไม่?
- ✅ มีค่าเริ่มต้นหรือไม่?

### **B. RLS Policies**
- ✅ มี policy ที่อนุญาตให้ user update ข้อมูลตัวเองหรือไม่?
- ✅ user ที่ login มีสิทธิ์ update หรือไม่?

### **C. ข้อมูลในตาราง**
- ✅ user ที่ login มีข้อมูลในตาราง profiles หรือไม่?
- ✅ ค่า credits ปัจจุบันคือเท่าไหร่?

## 🛠️ วิธีแก้ไข:

### **กรณีที่ 1: ไม่มีคอลัมน์ credits**
```sql
ALTER TABLE profiles ADD COLUMN credits INTEGER DEFAULT 20;
```

### **กรณีที่ 2: มีคอลัมน์ credit_balance แทน**
```sql
ALTER TABLE profiles RENAME COLUMN credit_balance TO credits;
```

### **กรณีที่ 3: ไม่มี RLS Policy**
```sql
CREATE POLICY "Users can update own profile" ON profiles
  FOR UPDATE USING (auth.uid() = id);
```

### **กรณีที่ 4: ข้อมูล user ไม่มีใน profiles**
```sql
INSERT INTO profiles (id, full_name, credits) 
VALUES (auth.uid(), 'User Name', 20);
```

## 🎯 วิธีทดสอบหลังแก้ไข:
1. **รีสตาร์ท server**
2. **เข้าสู่ระบบ** ใหม่
3. **ตรวจสอบ console** ว่ามี error อะไรหรือไม่
4. **ลงประกาศงาน** และดูว่า:
   - เครดิตลดลง 5 ในฐานข้อมูล
   - UI อัปเดตทันที
   - ไม่มี error ใน console

## 📋 ไฟล์ที่เกี่ยวข้อง:
- `src/App.tsx` - ฟังก์ชัน addJob (เพิ่ม debug logs)
- `CHECK_CREDITS_COLUMN.sql` - SQL script ตรวจสอบคอลัมน์

---

**🚨 สำคัญ: ต้องตรวจสอบให้แน่ใจว่ามีคอลัมน์ `credits` ในตาราง profiles และมี RLS policy ที่อนุญาตให้ user update ข้อมูลตัวเอง!**
