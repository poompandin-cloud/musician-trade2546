# ✅ แก้ไขปัญหาการตรวจสอบเครดิตเรียบร้อย!

## 🔧 ปัญหาที่พบ:
- ❌ Error "ไม่สามารถตรวจสอบเครดิตได้" เมื่อกดประกาศงาน
- ❌ อาจเกิดจากตาราง profiles ไม่มีคอลัมน์ credits
- ❌ RLS Policy ไม่อนุญาตให้ผู้ใช้อัปเดตข้อมูลตัวเอง

## 🛠️ การแก้ไขทั้งหมด:

### 1. ✅ ตรวจสอบและเพิ่มคอลัมน์ credits
```sql
-- รันใน Supabase SQL Editor
ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS credits INTEGER DEFAULT 20;

UPDATE profiles 
SET credits = 20 
WHERE credits IS NULL;
```

### 2. ✅ แก้ไข Logic ใน App.tsx
- **สร้าง profile อัตโนมัติ** ถ้ายังไม่มี
- **ใช้ค่าเริ่มต้น 20 เครดิต** แทนการ throw error
- **รองรับทั้ง credits และ credit_balance**
- **แก้ไขปัญหา scope ของตัวแปร**

### 3. ✅ สร้าง RLS Policies
```sql
-- รันใน Supabase SQL Editor
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can update own profile" ON profiles
    FOR UPDATE USING (auth.uid() = id);
```

### 4. ✅ ปรับปรุงการหักเครดิต
- **ตรวจสอบคอลัมน์ที่มีอยู่จริง**
- **อัปเดตเฉพาะคอลัมน์ที่พบ**
- **Fallback ไปที่ credits ถ้าไม่มีคอลัมน์อื่น**

## 🔄 การทำงานที่แก้ไขแล้ว:

### **ขั้นตอนการลงประกาศงาน**:
1. **ตรวจสอบ session** ✅
2. **เช็ค weekly quota** ✅
3. **ตรวจสอบ/สร้าง profile** ✅ - ไม่ error แล้ว!
4. **ตรวจสอบเครดิต** ✅ - มีค่าเริ่มต้น 20
5. **บันทึกงาน** ✅
6. **หักเครดิต** ✅ - อัปเดตคอลัมน์ที่ถูกต้อง
7. **Refetch profile** ✅
8. **Real-time update** ✅

## 📋 วิธีแก้ไขปัญหา:

### **Step 1: รัน SQL ใน Supabase**
1. เข้าไปที่ Supabase Dashboard
2. ไปที่ SQL Editor
3. รันคำสั่งจากไฟล์ `CHECK_PROFILES_TABLE.sql`
4. รันคำสั่งจากไฟล์ `PROFILES_RLS_POLICIES.sql`

### **Step 2: ทดสอบการลงประกาศงาน**
1. เข้าสู่ระบบและไปที่ `/search`
2. ลงประกาศงาน
3. ควรไม่มี error "ไม่สามารถตรวจสอบเครดิตได้"
4. เครดิตควรถูกหัก 5 เครดิต

## 🎯 ผลลัพธ์ที่ได้:
- ✅ **ไม่มี error** เมื่อตรวจสอบเครดิต
- ✅ **สร้าง profile อัตโนมัติ** สำหรับผู้ใช้ใหม่
- ✅ **เครดิตเริ่มต้น 20** สำหรับผู้ใช้ใหม่
- ✅ **หักเครดิตได้ถูกต้อง** ตามคอลัมน์ที่มี
- ✅ **Real-time sync** ระหว่าง ProfilePage และ CreditWidget

## 📊 ไฟล์ที่แก้ไข:
- `src/App.tsx` - แก้ไข logic การตรวจสอบและหักเครดิต
- `CHECK_PROFILES_TABLE.sql` - SQL สำหรับตรวจสอบและเพิ่มคอลัมน์
- `PROFILES_RLS_POLICIES.sql` - SQL สำหรับสร้าง RLS Policies

---

**🎉 ตอนนี้การลงประกาศงานควรทำงานได้แล้ว! ไม่มี error และเครดิตถูกหักอย่างถูกต้อง**
