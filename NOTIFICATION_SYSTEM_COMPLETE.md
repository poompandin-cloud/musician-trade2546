# 🔔 ระบบแจ้งเตือนแบบ Real-time พร้อม NotificationBell

## สิ่งที่ได้สร้าง:

### 1. 🗄️ ตาราง notifications (Database)
```sql
CREATE TABLE notifications (
    id UUID PRIMARY KEY,
    user_id UUID NOT NULL,
    type VARCHAR(50) NOT NULL, -- 'job_application', 'job_confirmed', 'review_received'
    title TEXT NOT NULL,
    message TEXT NOT NULL,
    related_id UUID,
    is_read BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

### 2. 🛡️ RLS Policies ความปลอดภัย
- **SELECT**: ผู้ใช้อ่านการแจ้งเตือนของตัวเองได้
- **INSERT**: ระบบสามารถสร้างการแจ้งเตือนได้
- **UPDATE**: ผู้ใช้อัปเดตการแจ้งเตือนของตัวเองได้
- **DELETE**: ผู้ใช้ลบการแจ้งเตือนของตัวเองได้

### 3. ⚡ Automatic Triggers
- **Job Application**: สร้าง notification เมื่อมีคนสมัครงาน
- **Job Confirmed**: สร้าง notification เมื่อการสมัครได้รับการยืนยัน
- **Review Received**: สร้าง notification เมื่อได้รับรีวิว

### 4. 🎨 NotificationBell Component
**Features:**
- **Real-time Updates**: รับ notification ใหม่ทันที
- **Unread Count Badge**: แสดงจำนวน notification ที่ยังไม่อ่าน
- **Dropdown Panel**: แสดงรายการ notification ละเอียด
- **Mark as Read**: ทำเครื่องอ่านทั้งหมดหรือรายการเดียว
- **Delete**: ลบ notification ที่ไม่ต้องการ
- **Mobile Responsive**: รองรับมือถือ 100%

### 5. 🪝 useNotifications Hook
**Functions:**
- `fetchNotifications()`: ดึงข้อมูล notification
- `markAsRead()`: ทำเครื่องอ่านทั้งหมด
- `markSingleAsRead()`: ทำเครื่องอ่านรายการเดียว
- `deleteNotification()`: ลบ notification
- `refreshNotifications()`: รีเฟรชข้อมูล

## การทำงานของระบบ:

### 🔄 Real-time Flow:
1. **คนสมัครงาน** → Trigger สร้าง notification ให้เจ้าของงาน
2. **เจ้าของงานยืนยัน** → Trigger สร้าง notification ให้ผู้สมัคร
3. **รีวิวถูกสร้าง** → Trigger สร้าง notification ให้คนที่ถูกรีวิว
4. **Real-time Subscription** → Component รับข้อมูลทันที

### 📱 UI Features:
- **Bell Icon**: ไอคอนกระดิ่งพร้อม badge จำนวน
- **Unread Indicator**: พื้นหลังสีฟ้านสำหรับ notification ที่ยังไม่อ่าน
- **Time Formatting**: แสดงเวลาแบบสัมพัทธ์ (เมื่อกี้วี่, 5 นาทีที่แล้ว)
- **Type Icons**: ไอคอนตามประเภท notification (👥, ✅, ⭐)
- **Responsive Design**: ทำงานบนทุกขนาดหน้าจอ

## วิธีติดตั้ง:

### 1. รัน SQL:
```bash
# รันใน Supabase SQL Editor
ไฟล์: supabase/notifications_system.sql
```

### 2. เพิ่ม NotificationBell ใน App:
```tsx
import NotificationBell from "@/components/NotificationBell";

// ในส่วน header หรือ navigation
<NotificationBell userId={currentUserId} />
```

### 3. ทดสอบ Real-time:
1. สร้างงานใหม่
2. ให้คนอื่นสมัครงาน
3. สังเกต notification ที่ปรากฏทันที
4. ทดสอบการยืนยันและรีวิว

## ประเภท Notification:

### 📥 Job Application:
- **Trigger**: มีคนสมัครงานใหม่
- **Recipient**: เจ้าของงาน
- **Icon**: 👥
- **Message**: "มีคนสมัครงาน [ชื่องาน] ของคุณแล้ว"

### ✅ Job Confirmed:
- **Trigger**: การสมัครงานได้รับการยืนยัน
- **Recipient**: ผู้สมัครงาน
- **Icon**: ✅
- **Message**: "การสมัครงาน [ชื่องาน] ของคุณได้รับการยืนยันแล้ว"

### ⭐ Review Received:
- **Trigger**: มีคนรีวิวใหม่
- **Recipient**: คนที่ถูกรีวิว
- **Icon**: ⭐
- **Message**: "คุณได้รับรีวิวสำหรับงาน [ชื่องาน]"

## Performance & Security:

### 🚀 Performance:
- **Indexes**: สร้าง index บนคอลัมน์ที่ใช้บ่อย
- **RPC Functions**: ใช้ stored procedures ลดจำนวน query
- **Real-time Subscription**: ใช้ Postgres Changes API
- **Caching**: เก็บข้อมูลไว้ใน state ของ component

### 🛡️ Security:
- **RLS Enabled**: ป้องกันการเข้าถึงข้อมูลผิดสิทธิ
- **User Isolation**: ผู้ใช้เห็นเฉพาะ notification ของตัวเอง
- **Input Validation**: ตรวจสอบข้อมูลก่อนบันทึก
- **SQL Injection Protection**: ใช้ parameterized queries

## ไฟล์ที่สร้าง:
- **`supabase/notifications_system.sql`** - Database schema, triggers, functions
- **`src/components/NotificationBell.tsx`** - React component สำหรับแสดง notification
- **`src/hooks/useNotifications.ts`** - Custom hook สำหรับจัดการ notifications

ตอนนี้ระบบมี notification แบบ real-time ครบถ้วน! 🎵✨
